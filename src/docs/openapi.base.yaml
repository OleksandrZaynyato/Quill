openapi: 3.0.0
info:
  title: My API
  version: 1.0.0

servers:
  - url: https://{environment}/api
    description: API server
    variables:
      environment:
        default: localhost:5000
        enum:
          - localhost:5000
          - quill-zb7e.onrender.com


paths: {}        # will be merged
components:
  schemas: {}     # will be extended
  securitySchemes: {}
